# 🚀 MediaBot - Быстрый старт

## Что у нас есть?

```
📁 Проект MediaBot
├── 🔧 Локальная разработка    (make dev)
├── 🧪 Staging тестирование    (make staging)
├── 🚀 Production деплой       (make prod)
└── 📊 Мониторинг             (Grafana + Prometheus)
```

## Простые команды

### Начать работу

```bash
# Запустить локальную разработку
make dev

# Посмотреть что происходит
make status

# Посмотреть логи
make dev-logs
```

### Деплой

```bash
# На staging (для тестирования)
make staging

# На production (боевой)
make prod
```

### Если что-то сломалось

```bash
# Посмотреть статус
make status

# Очистить все и начать заново
make clean
make dev
```

## Файлы конфигурации

| Файл                      | Для чего                       |
| ------------------------- | ------------------------------ |
| `.env`                    | Локальная разработка           |
| `.env.staging`            | Тестирование перед продакшеном |
| `.env.production`         | Боевой сервер                  |
| `docker-compose.yml`      | Основная конфигурация          |
| `docker-compose.dev.yml`  | Дополнения для разработки      |
| `docker-compose.prod.yml` | Продакшен конфигурация         |

## Порты и адреса

### Разработка (make dev)

- 🌐 Frontend: http://localhost:3000
- 🔌 API: http://localhost:8080
- 🤖 n8n: http://localhost:5678
- 🗄️ PostgreSQL: localhost:5433
- 📊 Grafana: http://localhost:3001

### Production

- 🌐 Все: http://158.160.190.4

## Что делать если...

### 🔥 Проект не запускается

```bash
make clean
make check-env
make dev
```

### 🐛 Нужно посмотреть ошибки

```bash
make dev-logs
# или
make prod-logs
```

### 💾 Нужен backup

```bash
make backup
```

### 🔄 Обновить production

```bash
# 1. Сначала протестировать на staging
make staging

# 2. Если все ок, деплоить на prod
make prod
```

## Структура проекта

```
mediabot/
├── 📱 frontend/           # React приложение
├── 🔌 backend/            # Node.js API
├── 🤖 n8n-workflows/      # Автоматизация
├── 🐳 docker-compose.*    # Конфигурации Docker
├── 📝 .env.*             # Переменные окружения
├── 🛠️ scripts/           # Полезные скрипты
└── 📊 monitoring/        # Grafana, Prometheus
```

## Безопасность

✅ Все секреты в `.env` файлах
✅ Разные пароли для разных окружений
✅ HTTPS на продакшене
✅ Регулярные backup'ы
✅ Мониторинг и алерты

## Помощь

```bash
# Показать все доступные команды
make help

# Проверить статус
make status

# Проверить конфигурацию
make check-env
```

---

**💡 Совет:** Начинайте всегда с `make dev`, тестируйте на `make staging`, деплойте на `make prod`
