# Claude Code SuperClaude Framework - Developer Cheat Sheet

## 🚀 Core Slash Commands

### Development & Implementation

```bash
/build [target]                    # Framework-aware project building
/implement [feature] --type [api|component|service]  # Feature implementation
/design [domain]                   # System/UI design orchestration
```

### Analysis & Investigation

```bash
/analyze [target] --focus [performance|security|quality]  # Multi-dimensional analysis
/troubleshoot [symptoms]           # Root cause investigation
/explain [topic] --level [basic|intermediate|advanced]    # Educational explanations
```

### Quality & Improvement

```bash
/improve [target] --loop           # Iterative enhancement (auto 3 cycles)
/cleanup [target]                  # Technical debt reduction
/refactor [target]                 # Code quality improvement
```

### Testing & Validation

```bash
/test [type] --e2e                 # Testing workflows with Playwright
/validate [target]                 # Pre-operation validation
```

### Documentation & Planning

```bash
/document [target]                 # Professional documentation
/estimate [target]                 # Evidence-based estimation
/task [operation]                  # Long-term project management
```

### Version Control & Deployment

```bash
/git [operation]                   # Git workflow assistant
/deploy [target]                   # Deployment orchestration
```

---

## 🎯 Power Flags (Auto-Activate Intelligence)

### Thinking Depth

```bash
--think                    # Multi-file analysis (~4K tokens)
--think-hard              # Deep architectural analysis (~10K tokens)
--ultrathink              # Critical system redesign (~32K tokens)
```

### MCP Server Control

```bash
--c7 / --context7         # Library documentation lookup
--seq / --sequential      # Complex multi-step analysis
--magic                   # UI component generation
--play / --playwright     # Browser automation & testing
--all-mcp                 # Enable all servers (high token usage)
--no-mcp                  # Native tools only (40-60% faster)
```

### Wave Orchestration (Multi-Stage Execution)

```bash
--wave-mode [auto|force|off]       # Control wave activation
--wave-strategy [progressive|systematic|adaptive|enterprise]
--wave-delegation [files|folders|tasks]
```

### Task Delegation (Parallel Processing)

```bash
--delegate [files|folders|auto]    # Sub-agent delegation
--concurrency [n]                  # Max concurrent agents (1-15)
```

### Performance & Efficiency

```bash
--uc / --ultracompressed          # 30-50% token reduction
--safe-mode                       # Maximum validation
--validate                        # Pre-operation risk assessment
--loop                            # Iterative improvement (auto 3 cycles)
--iterations [n]                  # Custom iteration count
```

### Scope & Focus

```bash
--scope [file|module|project|system]
--focus [performance|security|quality|architecture|accessibility]
```

### Persona Activation

```bash
--persona-architect               # Systems design specialist
--persona-frontend               # UX/accessibility expert
--persona-backend                # Reliability engineer
--persona-security               # Threat modeling expert
--persona-analyzer               # Root cause specialist
--persona-performance            # Optimization expert
--persona-qa                     # Quality advocate
--persona-refactorer            # Code quality specialist
--persona-devops                # Infrastructure expert
--persona-mentor                # Knowledge transfer
--persona-scribe=lang           # Professional writer
```

---

## 🔥 Combo Power Commands

### Full Stack Analysis

```bash
/analyze --think-hard --all-mcp --focus architecture
/analyze --wave-mode --systematic --focus security
```

### Advanced Implementation

```bash
/implement "user dashboard" --magic --c7 --persona-frontend
/build --wave-strategy progressive --delegate auto
```

### Quality Improvement Campaigns

```bash
/improve --loop --wave-mode --focus quality --persona-refactorer
/cleanup --think --seq --iterations 5
```

### Performance Optimization

```bash
/improve --focus performance --think-hard --persona-performance --play
/analyze --focus performance --wave-mode --validation
```

### Security Hardening

```bash
/analyze --focus security --ultrathink --persona-security --validate
/improve --focus security --wave-strategy systematic --safe-mode
```

---

## 📊 Intelligence Auto-Triggers

### Wave Mode Auto-Activates When:

- **Complexity ≥ 0.7** + **Files > 20** + **Operation types > 2**
- Keywords: "comprehensive", "systematically", "enterprise", "large-scale"

### Personas Auto-Activate On:

- **Security**: "vulnerability", "auth", "compliance" → `--persona-security`
- **Performance**: "slow", "optimize", "bottleneck" → `--persona-performance`
- **Frontend**: "component", "UI", "responsive" → `--persona-frontend`
- **Architecture**: "design", "scalability", "system" → `--persona-architect`

### MCP Servers Auto-Activate On:

- **Context7**: Import statements, framework questions
- **Sequential**: Complex debugging, system design, `--think` flags
- **Magic**: UI component requests, design queries
- **Playwright**: Testing, E2E, performance monitoring

---

## 🎪 MediaBot Project Specific

### n8n Workflow Development

```bash
/implement "n8n chat workflow" --seq --c7 --persona-backend
/analyze n8n-workflows/ --think --focus "GPT integration"
```

### Full System Integration

```bash
/analyze --wave-mode --focus "n8n integration" --all-mcp
/improve --wave-strategy adaptive --focus "business_id isolation"
```

### Security Audit (Pre-Production)

```bash
/analyze --focus security --wave-mode --validation --persona-security
/improve --focus security --safe-mode --think-hard
```

### Performance Optimization

```bash
/improve --focus performance --wave-strategy progressive --play
/analyze --focus performance --think-hard --persona-performance
```

---

## 🚨 Emergency Protocols

### Resource Management Thresholds

- **Green** (0-60%): Full operations
- **Yellow** (60-75%): Auto-enable `--uc`
- **Red** (85-95%): Force efficiency modes
- **Critical** (95%+): Essential operations only

### Quick Recovery

```bash
--safe-mode                       # Maximum validation
--no-mcp --uc                    # Fastest execution
--validate --think               # Thorough but safe analysis
```

---

## 💡 Pro Tips

1. **Start Simple**: Let auto-activation handle complexity
2. **Combine Flags**: `--think --seq --persona-analyzer` for deep debugging
3. **Use Wave Mode**: For system-wide changes (auto-activates intelligently)
4. **Leverage Personas**: They know their domains better than generic approaches
5. **Trust Auto-Detection**: Framework is optimized for intelligent flag activation

**Remember**: Just describe what you need - the framework intelligently activates appropriate features!

---

## 🔗 Quick Reference Links

- **CLAUDE.md**: Project-specific instructions and context
- **Commands**: `.claude/commands/` for custom MediaBot commands
- **Documentation**: All framework details in `~/.claude/` files

**Last Updated**: August 2025 - MediaBot Project Context
